mixin select-tree(categories, level=2)
    if(categories)
        each item in categories
            - const prefix = Array(level).join('--')
            option(value=item.id) #{prefix} #{item.title} 
            if(item.child)
                +select-tree(item.child, level+1)
                
